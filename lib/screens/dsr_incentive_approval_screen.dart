import 'package:flutter/material.dart';

class DSRIncentiveApproval extends StatelessWidget {
  static const String id = 'DSRIncentiveApproval';
  const DSRIncentiveApproval({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Incentive Approval'),
      ),
      body: const Center(child: Text('Hello World')),
    );
  }
}
